'use strict';

describe('myApp.motherDash module', function() {

  beforeEach(module('myApp.motherDash'));

  describe('motherDash controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var MotherCtrl = $controller('MotherCtrl');
      expect(MotherCtrl).toBeDefined();
    }));

  });
});