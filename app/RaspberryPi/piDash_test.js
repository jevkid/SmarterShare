'use strict';

describe('myApp.piDash module', function() {

  beforeEach(module('myApp.piDash'));

  describe('Pi controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PiCtrl = $controller('PiCtrl');
      expect(PiCtrl).toBeDefined();
    }));

  });
});